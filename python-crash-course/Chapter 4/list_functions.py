strings = ['one', 'two', 'three']
numbers = [1, 2, 3]

# min(strings) does not work! supports rich comparison
min(numbers)

# max(strings) does not work! supports rich comparison
max(numbers)

# sum(strings) does not work! supports rich comparison
sum(numbers)